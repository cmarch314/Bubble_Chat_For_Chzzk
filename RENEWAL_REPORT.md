# BubbleChat 전면 리뉴얼 결과

기준일: 2026-07-15  
작업 브랜치: `codex/full-rebuild`

## 최우선 원칙

기존 OBS 채팅 오버레이, 시각 효과, 채팅 영상, SFX, 미니게임, 수렵 콘텐츠를 유지하면서 장애 복구와 변경 안전성을 먼저 확보했다. 대형 파일을 한 번에 다시 쓰는 방식은 기능 회귀를 감지하기 어렵기 때문에, 동작 계약을 만든 뒤 연결·입력·오디오·수명주기·레이아웃·자산 순서로 독립 커밋했다.

## 완료한 페이즈

### 1. 복구 기반

- 리뉴얼 전 전체 Git 이력 번들 및 추적 소스 ZIP 생성
- 현재 미추적 런타임 미디어 1,591개를 별도 ZIP으로 백업하고 파일 수와 원본 총 바이트 일치 검증
- 기능별 독립 커밋으로 즉시 비교·되돌리기 가능한 체크포인트 구성

### 2. 치지직 연결 복구

- WebSocket 연결과 치지직 인증 완료를 분리
- 인증 응답 전에는 연결 성공 이벤트를 발생시키지 않음
- 잘못된 캐시 자동 폐기 및 현재 방송 채팅 세션 재탐색
- 지수 백오프와 지터를 적용한 제한 재시도
- 방송 재시작으로 내부 채팅 세션이 바뀌면 새 소켓 인증 후 기존 소켓을 교체
- 일상적인 복구에서는 OBS 페이지, 게임, 효과, 채팅 버블을 새로고침하지 않음
- 작동 중 연결은 상태 확인 요청 실패만으로 종료하지 않음

### 3. 원격 입력 안전화

- 채팅, 닉네임, 참가자, 우승자 문자열을 기본 텍스트로 처리
- 이모트는 이스케이프된 텍스트와 검증된 HTTPS 이미지만 허용
- 원격 색상값과 이미지 URL 검증
- 수렵, 레이드, 경마, 퀴즈, 일반 채팅 렌더링 경로에 동일 정책 적용

### 4. 체감 음량 균일화

- 원본을 수정하지 않고 1,411개 미디어의 LUFS·피크를 측정
- 파일별 측정 보정을 SFX, 영상, BGM에 적용
- 컴프레서는 여러 효과 충돌과 피크 보호 역할로 조정
- OBS `file://` 환경에서 DOM 미디어를 Web Audio로 연결하지 않아 CORS 무음 문제 방지
- 모든 프로그래밍 방식 오디오 생성을 중앙 관리자 경로로 통합

### 5. 수명주기 정리

- 효과 실행마다 전용 타이머 범위를 만들고 성공·실패·중단 시 정리
- 채팅, 비주얼 디렉터, 앱 시작 시퀀스, 수렵 렌더러·오디오 타이머를 소유자와 함께 종료
- 오래된 애니메이션 콜백이 다음 게임 화면을 오염시키는 경로 차단
- 이벤트 구독과 채팅 버블도 명시적 `dispose()`에서 정리

### 6. 1920×1080 OBS 계약

- 투명 배경과 화면 스크롤 금지
- 채팅을 최하단에 고정하고 모든 효과보다 높은 레이어 유지
- 게임은 상단 80%, 채팅은 하단 20%를 사용
- 게임 도움말을 1080p 무스크롤 2열 레이아웃으로 변경
- 위 규칙을 자동 회귀 테스트로 고정

### 7. 런타임 자산 무결성

- 설정에 연결된 1,428개 고유 미디어 파일 검사
- 78개 채팅 영상 명령과 실제 MP4 일치 검사
- 1,411개 음량 프로필과 실제 파일 일치 검사
- 검사 과정에서 발견한 두 개의 잘못된 시각 효과 음원 경로 수정
- `npm run verify` 하나로 문법·UTF-8·CSS·HTML 리소스·런타임 미디어를 함께 검사

### 8. 구조 분해와 로컬 연결 경로

- 단일 설정 파일을 런타임·사운드·시각 효과·채팅 영상 카탈로그로 분리하고 로딩 순서를 테스트로 고정
- 경마 데이터·규칙·시뮬레이션·표현을 분리하고 수렵 계산·전투 틱·턴 실행·특수 몬스터·알림·전투 애니메이션·결과 연출을 독립 모듈로 분리
- 도움말·명령어 스크롤·레이드의 동적 스타일 생성을 정적 스타일시트로 이전
- 프리로더·오디오·디버그 타이머를 앱 종료 수명주기에 연결
- OBS 시작·종료 수명주기에 결합된 로컬 보조 실행기를 추가해 평상시 상주 프로세스 없이 치지직 상태·인증 API에 접근
- 방송 세션 변경 시 페이지 새로고침 없이 새 채팅 소켓을 인증한 뒤 기존 소켓과 교체

## 검증 결과

- 전체 자동 테스트 통과
- 프로젝트 무결성 검사 통과
- 런타임 미디어 검사 통과
- Git diff 공백·패치 오류 검사 통과
- 기존 미추적 미디어와 복구용 파일은 삭제·이동하지 않음
- OBS Lua 자동 연동 등록 후 실제 접속 확인 완료

## 남은 구조적 위험과 다음 원칙

- `HuntEngine`의 무기별 전투 규칙은 여전히 가장 큰 결합 지점이다. 계산을 바꿀 때는 `HuntMonsterRules`처럼 순수 규칙과 고정 시드 테스트를 먼저 만든 뒤 한 무기군씩 이동해야 한다.
- 헌트 참가자 카드에 표시되는 외부 닉네임과 색상도 `SafeContent` 경계를 통과하도록 보강해, 결과/장비 표시 템플릿을 통한 마크업 및 CSS 주입 경로를 차단했다.
- 로컬 보조 실행기가 꺼지면 기존 직접·외부 중계 폴백으로 연결되므로 기능은 유지되지만 최초 연결 시간은 다시 외부 서비스 상태에 영향을 받을 수 있다. 방송 전 `START_OBS_OVERLAY.bat` 실행을 기본 운영 절차로 유지한다.
- 자동 테스트는 DOM 계약과 수명주기를 검증하지만 OBS의 실제 폰트 렌더링·GPU 합성 픽셀까지 비교하지는 않는다. 실제 OBS에서 채팅·도움말·수렵·경마 기준 캡처를 보관하면 이후 시각 회귀의 마지막 공백을 메울 수 있다.

운영 설정과 장애 대응 방법은 [OPERATIONS.md](OPERATIONS.md)를 따른다.
