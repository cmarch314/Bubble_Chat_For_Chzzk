# 수렵 오토배틀러 로그라이크 마스터 플랜

상태: 실행 초안 v0.3  
기준일: 2026-07-22  
문서 위계: 수렵 프로젝트의 최상위 제품·개발 계획

이 문서는 `HUNT_AUTOBATTLER_DESIGN.md`, `HUNT_PERK_DESIGN.md`와 개별 구현 보고서보다 우선한다. 하위 문서와 구현이 이 계획과 충돌하면 이 문서를 먼저 갱신한 뒤 작업한다.

## 0. 최상위 불변 원칙: 로그라이크 × 채팅 커맨드

이 프로젝트의 기본 구성은 **로그라이크 방식의 여정 진행**과 **채팅 커맨드를 통한 플레이어 입력**의 결합이다. 이후의 모든 마일스톤과 기능은 이 두 축을 전제로 설계하고 검수한다.

- 전투, 이벤트, 보상, 자원 관리, 성장과 분기는 한 번의 여정 안에서 누적되는 로그라이크 구조로 구성한다.
- 시청자의 주요 입력 수단은 별도 조작 화면이 아닌 방송 채팅 커맨드다.
- 선택지는 원칙적으로 `!1`, `!2`, `!3`처럼 짧고 화면에서 즉시 읽히는 명령으로 제공한다.
- 파티 공동 선택은 투표로 결정하며, 동표와 무응답은 시드 기반 자동 선택으로 진행이 멈추지 않게 한다.
- 헌터별 선택이 의미 있는 이벤트는 참가자별 입력을 따로 적용하고, NPC와 무응답 헌터는 자동 선택한다.
- 채팅이 없어도 자동 전투와 기본 선택으로 여정이 끝까지 진행되어야 한다.
- 새 기능이 이 원칙과 충돌하면 로그라이크 축적성과 채팅 참여성을 보존하는 방향으로 단순화한다.

### 마일스톤 공통 검수 질문

1. 현재 여정의 자원·성장·위험 선택에 실제로 누적되는가?
2. 시청자가 짧은 채팅 커맨드로 이해하고 참여할 수 있는가?
3. 무응답 상태에서도 OBS 화면과 게임 진행이 멈추지 않는가?
4. 저장 후 재개해도 투표·선택·보상이 동일하게 복원되는가?

## 1. 궁극적 목표

수렵 프로젝트의 최종 장르는 **채팅 참여형 오토배틀러 로그라이크**다. 짧게 한 판을 확인하는 단일 수렵과 자원을 이어 가는 여정형 수렵은 같은 전투 엔진을 쓰되 서로 다른 제품 모드로 취급한다.

네 명의 헌터는 자동으로 싸우고 생존 판단을 내린다. 시청자는 매 공격을 조작하는 대신 무기 성장, 퍽 보존, 이벤트 선택과 한정된 지원 행동에 참여한다. 한 번의 런 동안 체력과 아이템이 이어지고, 불완전한 선택들이 누적되어 마지막 초대형 몬스터를 쓰러뜨리는 것이 목표다.

핵심 재미는 세 가지다.

1. **자원 관리**: 체력·물약·가루·함정·폭탄·수레·제니를 다음 전투까지 고려해 사용한다.
2. **빌드 성장**: 좋은 퍽을 잠그고 나머지를 다시 굴려 궁극의 조합을 만든다.
3. **무기 성장**: 철·뼈 계통에서 출발해 쓰러뜨린 몬스터 계통에 맞는 실제 무기명과 성능으로 강화한다.

모든 시스템은 다음 원칙을 지킨다.

- 규칙은 깊되 화면과 선택지는 짧다.
- 한 노드에서 시청자가 이해해야 하는 핵심 선택은 최대 2개다.
- 무응답이어도 AI 추천안으로 런이 멈추지 않는다.
- 숫자는 가능한 한 한 자리 자연수로 표현한다.
- 원작 데이터는 정체성을 제공하고, 플레이 규칙은 방송에 맞게 압축한다.
- **몬스터는 분석표가 아니라 신비로운 존재로 남아야 한다.** 능력·상태·행동 정보를 상시 설명하지 않으며, 그래픽만으로 판독하기 어려운 위험이나 일시 상태에 한해서만 이모지와 최소한의 수치 신호를 예외적으로 제공한다.
- OBS 새로고침이나 채팅 단절이 런을 파괴하지 않는다.

### 명령어 제품 계약

| 명령 | 모드 | 수명 | 저장 적용 | 종료 |
|---|---|---|---|---|
| `!수렵 [몬스터] [무기문자]` | 단일 수렵 | 퀘스트 1회 | 시청자 프로필을 시작 장비로 불러오고 확정 변경을 저장 | 결과 화면 뒤 완전 종료 |
| `!몬헌` | 여정형 수렵 | 3스테이지 15노드 | 프로필과 별도로 활성 런 체크포인트 저장 | 승리·실패·명시적 포기 때 종료 |
| `!토벌` | 단일 수렵 호환 별칭 | `!수렵`과 동일 | 동일 | 동일 |

- `!수렵`의 몬스터 지정과 기존 대연속 숫자 문법은 호환을 위해 유지하지만 여정 자원에는 손대지 않는다.
- `!몬헌` 뒤의 임의 몬스터·무기 문자는 받지 않는다. 여정의 시드와 노드 덱이 조우를 결정한다.
- 활성 런이 있으면 `!몬헌`은 마지막 확정 체크포인트를 이어서 연다. 새 런 강제 시작과 포기는 별도 스트리머 확인 명령을 구현하기 전까지 자동 추론하지 않는다.
- 두 명령 모두 같은 `HuntEffect` 전투 하위 흐름을 사용한다. 모드 분기는 파싱 결과의 `huntMode`로 전달하며 화면 문자열을 보고 다시 추론하지 않는다.

현재 구현 상태: 명령 라우팅·`huntMode` 분리·프로필 DB 연동은 완료됐다. `!몬헌`의 전체 노드 진행과 체크포인트 재개는 아래 L2~L6이 완료돼야 제품 계약을 완전히 만족한다.

## 2. 한 런의 기본 구조

한 런은 총 3스테이지, 15개 노드로 구성한다. 전투 9회와 이벤트 6회이며 모든 전투 사이에는 랜덤 이벤트가 정확히 하나씩 들어간다.

| 스테이지 | 노드 순서 | 역할 |
|---|---|---|
| 1. 개척 | 소형 몬스터 → 이벤트 → 중형 몬스터 → 이벤트 → 대형 몬스터 | 파티의 기본 빌드와 첫 무기 계통 확립 |
| 2. 격전 | 중형 몬스터 → 이벤트 → 대형 몬스터 → 이벤트 → 고룡급 몬스터 | 자원 압박과 빌드 완성 방향 결정 |
| 3. 결전 | 대형 몬스터 → 이벤트 → 고룡급 몬스터 → 이벤트 → 초대형 몬스터 | 마지막 정비 후 축적한 빌드의 폭발 |

스테이지가 오를 때마다 시작 티어도 한 단계씩 상승한다. 각 스테이지의 마지막 전투는 고정 보스이며, 앞선 전투와 이벤트는 2~3개 선택지를 통한 투표 구조를 유지한다.

### 노드 공통 흐름

```text
노드 공개
→ 짧은 위험도·보상 예고
→ 자동 전투 또는 최대 2개 선택지 투표
→ 결과와 자원 변화 정산
→ 런 저장
→ 다음 노드
```

현재의 `quest_board → loadout → fighting → results`는 폐기하지 않는다. 하나의 전투 노드를 실행하는 하위 흐름으로 재사용하고, 그 위에 전체 런을 소유하는 `HuntRunDirector`를 둔다.

## 3. 런 자원

### 계속 유지되는 자원

- 헌터별 현재 체력
- 캠프 공용 보급품: 물약, 치유가루, 함정, 폭탄
- 파티 전체 수레 잔여량
- 제니
- 현재 무기와 강화 계통
- 현재 퍽, 잠긴 퍽, 리롤 기회

전투가 끝났다는 이유만으로 체력과 소모품을 전부 회복하지 않는다. 회복은 이벤트, 거래, 스테이지 보상 또는 퍽의 실제 효과로 얻는다.

물약은 런 시작 때 캠프 공용 재고 10개로 출발한다. 모든 헌터가 같은 재고에서 소비하며 채집, 상점, 랜덤 이벤트 보상으로 다시 채울 수 있다. 공용 물약의 기본 상한은 10개다.

### 전투 사이 자동 정비

- 숫돌질과 재장전처럼 소모 재화가 없는 정비는 자동 수행한다.
- 상태이상과 일시 버프는 전투 종료 시 해제한다.
- 수레나 귀환옥으로 캠프에 복귀해도 공용 보급품을 새로 만들지 않는다. 무기 상태만 정비하고 남은 공용 재고를 그대로 사용한다.
- 스테이지 진입 시 소량의 기본 보급만 제공하며 완전 회복은 보장하지 않는다.

### 제니

제니는 `0~9` 범위의 작은 자연수로 관리한다. UI에는 큰 금액이나 쉼표를 표시하지 않는다.

| 변화 | 기본값 |
|---|---:|
| 중형 몬스터 승리 | +1 |
| 대형 몬스터 승리 | +2 |
| 고룡급·초대형 승리 | +3 |
| 일반 거래 | 1 |
| 강한 거래·리롤 | 2 |
| 무기 강화·희귀 보상 | 3 |

실제 밸런스 수치는 시뮬레이션으로 조정하되, 비용 표현은 계속 `1·2·3` 중심으로 유지한다.

### 로컬 저장 경계와 자원 예산

기존 `127.0.0.1:17890` OBS 동반 서버를 재사용한다. 브라우저가 별도 DB 라이브러리를 들고 있거나 전체 서버 데이터를 복제하지 않는다.

```text
OBS 브라우저
  HuntProfileClient (세션 캐시 + 700ms 지연 저장)
        ↓ GET/POST /api/hunt-profile
로컬 동반 서버
  HuntProfileStore (SQLite 1연결, WAL, synchronous=NORMAL)
        ↓
  .runtime/hunt-profiles.sqlite (git 무시)
```

시청자 프로필 한 행에는 안정 ID만 둔다.

```json
{
  "weaponId": "long_sword",
  "weaponInstanceId": 123,
  "personality": "veteran",
  "perkIds": ["perk_014", "perk_031"],
  "lockedPerkId": "perk_014"
}
```

- 퍽 설명, 효과 수치, 무기 예리도와 이름은 저장하지 않고 기존 런타임 카탈로그에서 ID로 다시 찾는다.
- Chzzk UID를 우선 키로 쓰고 UID가 없는 로컬·테스트 입력만 정규화 닉네임으로 대체한다. DB 기본키는 원문 대신 SHA-256 해시다.
- NPC는 읽거나 저장하지 않는다. 사람 헌터는 로드아웃 진입 때 세션당 1회 읽고, 실제 변경 때만 지연 저장하며 전투 출발 직전 확정 저장한다.
- 동반 서버가 꺼졌거나 500ms 안에 응답하지 않으면 현재 랜덤 로드아웃으로 계속 진행한다. 저장 장애가 방송을 멈추면 안 된다.
- DB는 동반 서버가 첫 프로필 요청을 받을 때 지연 생성한다. 폴링, 전투 틱 쓰기, 전체 카탈로그 직렬화는 금지한다.
- 명령 별칭·중단·도움말은 `HuntCommandCatalog`, 프로필 허용 필드·최대 퍽·API 경로는 `HuntProfileContract`만 수정한다. 소비 파일에 같은 목록을 다시 적지 않는다.
- 로컬 서버 주소는 `LocalCompanionEndpoint`가 소유하며 Chzzk와 프로필 요청이 같은 포트 변경을 따라간다.
- SQLite 변경은 `PRAGMA user_version` 순차 마이그레이션으로만 수행한다. 지원 버전보다 새로운 DB는 자동 재작성하지 않는다.
- Node의 SQLite 기능이 없더라도 정적 오버레이와 Chzzk 프록시는 계속 살아 있어야 하며 프로필 API만 `503`으로 강등한다.
- 영구 프로필은 다음 방송의 시작점을 제공할 뿐 계정 영구 능력치 보너스는 주지 않는다. 로그라이크 런의 체력·제니·소모품은 별도 활성 런 레코드에만 둔다.

## 4. 퍽 성장

퍽은 런의 주 성장축이다. 새 퍽을 무한히 더하기보다 **좋은 퍽을 보존하고 나머지를 다시 굴리는 과정**을 강조한다.

| 구간 | 잠금 가능 수 | 기본 지급 |
|---|---:|---|
| 스테이지 1 | 1 | 잠금 기회 1회 |
| 스테이지 2 진입 | 2 | 잠금 기회 +1회 |
| 스테이지 3 진입 | 3 | 잠금 기회 +1회 |

규칙은 다음과 같다.

- 잠긴 퍽은 리롤에서 유지되고 목록 최상단에 모인다.
- 한 번 잠근 퍽은 해당 런에서 해제할 수 없다.
- 스테이지 잠금 상한은 각각 `1/2/3`이며 이벤트로도 현재 상한을 넘지 않는다.
- 이벤트는 리롤권 또는 남은 잠금 슬롯을 사용할 기회를 줄 수 있다.
- 리롤은 잠기지 않은 퍽만 다시 굴린다.
- 완성된 연계 퍽은 인접 배치와 연결 효과로 즉시 알아볼 수 있게 한다.
- `💩`처럼 희귀한 대박 퍽은 전투 시작에 발현하면 시청자에게 영구 귀속되어 리롤 뒤에도 유지한다.

잠금과 리롤 UI는 다음 정도로만 표현한다.

```text
🔒 2/3    🎲 1
```

## 5. 무기 제작과 성장

### 시작 선택

각 무기군은 희귀도 1의 두 뿌리 중 하나로 시작한다.

- **철 계통**: 안정적인 예리도·범용성
- **뼈 계통**: 높은 기초 공격·거친 예리도

표시 이름은 실제 게임의 한국어 무기명 데이터를 우선 사용한다. 예: 본블레이드, 본랜스, 아이언랜스 계열. 게임에 없는 이름을 그럴듯하게 생성해 정식 데이터처럼 표시하지 않는다.

### 압축된 강화 규칙

원작의 모든 소재를 인벤토리로 옮기지 않는다. 몬스터를 잡으면 그 몬스터가 속한 **계통 인장** 하나를 얻고, 대장장이 노드에서 현재 무기와 호환되는 최대 2개의 강화 후보를 보여준다.

초기 보상 계통은 다음처럼 압축한다.

- 철·광석
- 뼈
- 조룡·소형종
- 아룡종
- 비룡종
- 수생·해룡 계통
- 수룡·중량 계통
- 고룡 계통

실제 종 분류는 데이터에 보존하되 UI에서는 위 보상 계통만 보여준다. 한 몬스터가 여러 게임에서 다른 제작 분기를 갖는 경우, 현재 런의 무기군에서 실제로 이어지는 후보를 우선한다.

### 강화 결과

강화할 때 다음 항목이 함께 바뀐다.

- 실제 무기 이름
- 희귀도
- 무기 이미지 또는 외형 계통
- 공격력·회심·속성·상태이상
- 예리도 구성
- 무기별 특수 데이터: 포격형, 병, 탄, 코팅, 선율 등

한 런의 무기 단계는 네 구간이면 충분하다.

```text
R1 철/뼈 시작 무기
→ R2 계통 무기
→ R3 상위 계통 무기
→ R4 결전 무기
```

분기 수는 많아도 한 번에 보여주는 후보는 2개를 넘기지 않는다. 선택하지 않은 전체 트리는 도감에서만 확인한다.

### 스테이지별 강화 시나리오 초안

현재 Wilds 수집본은 14개 무기군의 실제 무기 1,188개를 담고 있다. 여정 투영 기준 분포는 `R1 113 / R2 175 / R3 281 / R4 619`이며, 모든 무기군에 철·뼈 시작점과 R4까지 도달 가능한 실제 제작 경로가 있다. 따라서 가짜 무기명이나 임의의 능력치 보간 없이도 한 런에 필요한 네 단계 성장을 구성할 수 있다.

핵심 리듬은 다음과 같다.

```text
런 시작       1스테이지 보스       2스테이지 보스       3스테이지 고룡 뒤
R1 뿌리 선택  → R2 계통 확립       → R3 주력 완성       → R4 결전 제련
철 / 뼈          첫 몬스터 분기         역할 특화             최종 보스 대응
```

| 시점 | 강화 장면 | 후보 구성 | 플레이 감정 |
|---|---|---|---|
| 런 시작 | 같은 무기군의 철·뼈 R1 중 선택 | 철은 안정적인 예리도, 뼈는 높은 공격력 중심 | 이번 런의 기본 성향 결정 |
| 1스테이지 대형 보스 격파 | 무료 R2 제작 | 현재 뿌리의 정통 후계 1개 + 획득 인장과 맞는 실제 분기 1개 | 평범한 장비가 몬스터 소재 무기로 변함 |
| 2스테이지 고룡급 보스 격파 | 무료 R3 제작 | 같은 계통의 상위형과 다른 역할의 실제 분기, 최대 2개 | 공격·회심·속성·상태이상 중 주력 방향 확정 |
| 3스테이지 고룡급 전투 뒤 마지막 이벤트 | R4 결전 제련 | 현재 계통의 완성형과 보유 인장으로 열린 결전형, 최대 2개 | 이름·외형·예리도가 크게 바뀌는 최종 완성 |
| 초대형 보스 격파 | 강화 없음 | 완성 무기로 결과 정산과 도감 등록 | 성장 결과를 시험하고 끝냄 |

스테이지 3의 두 번째 이벤트는 일반 이벤트 풀을 그대로 굴리지 않고 **결전 준비** 장면으로 승격한다. 이 장면 안에서 `R4 결전 제련 / 공용 보급 정비`를 보여주되, 제련을 고르면 실제 R4 무기명을 얻고 정비를 고르면 R3를 유지한 채 체력과 소모품 우위를 얻는다. 무기 완성을 강제하지 않으면서 마지막 선택이 초대형 전투의 전략으로 이어지게 한다.

#### 후보 생성 우선순위

후보는 점수만 높은 무기를 전체 목록에서 꺼내지 않는다. 다음 순서로 실제 제작 그래프를 따라간다.

1. 현재 무기와 같은 무기군이며 현재 노드에서 실제로 도달 가능한 후손만 남긴다.
2. 해당 시점의 정확한 목표 티어 `R2/R3/R4`만 허용한다.
3. 지금까지 얻은 계통 인장과 일치하는 분기를 우선한다.
4. 같은 역할의 숫자 차이 두 개가 나오면 하나를 제외하고 전투 역할이 다른 후보를 넣는다.
5. 후보가 하나뿐이면 실제 후보 하나만 보여준다. 존재하지 않는 두 번째 무기를 만들어 채우지 않는다.
6. 후보가 전혀 없으면 강화 장면을 보급 또는 제니 보상으로 자동 치환하고 런을 계속한다.

후보의 역할 표지는 수집된 필드에서 계산한다.

- `🗡️ 강공`: 높은 기초 공격, 낮은 회심 또는 짧은 예리도
- `✨ 예리`: 우수한 최고 예리도와 긴 유효 구간
- `🎯 회심`: 높은 회심과 안정적인 누적 피해
- `🔥💧⚡❄️🐉 속성`: 해당 속성 약점 공략
- `☠️⚡💤💥 상태`: 독·마비·수면·폭파 누적
- `🛡️ 수비`: 방어 보정이나 생존 성능

표지는 비교를 위한 요약일 뿐 별도 능력치를 발명하지 않는다. 실제 공격력·회심·속성·예리도와 무기별 특수 데이터가 유일한 근거다.

#### 무기별 특수 데이터 보호

일반 공격력 점수만으로 후보를 정렬하면 무기 정체성이 깨진다. 강화 후보와 적용 결과는 다음 데이터를 함께 보존한다.

- 건랜스의 포격형과 포격 레벨
- 차지액스·슬래시액스의 병 종류
- 수렵피리의 선율 구성
- 활의 병과 활 타입
- 라이트·헤비보우건의 탄종, 장전과 반동
- 쌍검의 두 속성처럼 복수 특성을 가진 예외

이 특수 데이터가 아직 전투 런타임에 연결되지 않은 무기는 UI에 효능이 있는 것처럼 표시하지 않는다. 연결된 필드만 비교 표지로 사용하고, 나머지는 수집 DB에 보존한 채 후속 구현 대상으로 남긴다.

#### 선택 화면과 자동 선택

헌터마다 자기 무기 후보를 최대 두 개만 받으며 채팅 입력은 `!1` 또는 `!2`로 통일한다. 화면에는 다음 다섯 정보만 표시한다.

```text
🐉 코로나  R4
공격 200　회심 10%　🔥 25　흰예리
현재 무기 대비  공격 +20 · 속성 +4
```

무응답과 NPC는 현재 빌드를 평가해 자동 선택한다. 속성 약점을 이미 알고 있는 경우에는 유효 속성을, 예리도 소모가 심한 성향에는 긴 예리도를, 고회심 연계 퍽이 있으면 회심 후보를 우선한다. 자동 선택은 숨은 보너스를 주지 않고 같은 두 후보 중 하나만 고른다.

#### 재화와 실패 방지

- 보스 뒤의 R2·R3 핵심 강화는 무료다. 코인 부족 때문에 기본 성장선이 끊기지 않는다.
- 대장간 랜덤 이벤트의 리롤·수리·옆 계통 전환은 `💰1~2`를 소비한다.
- R4 결전 제련은 고룡 인장 또는 현재 계통의 완성 경로로 해금하며 별도 큰 숫자의 재화를 만들지 않는다.
- 파티원이 후보를 얻지 못해도 다른 파티원의 강화는 정상 진행한다.
- 체크포인트에는 무기 진행 키와 선택 결과만 저장하고 전체 후보 목록은 시드와 카탈로그에서 다시 계산한다.

#### 구현 차이와 다음 작업

정책 파일의 스테이지 상한 `2/3/4`는 이 초안과 이미 일치한다. 반면 현재 `HuntJourneyWeaponGrowth`와 `HuntEffect`는 1·2스테이지 보스 뒤 두 번만 강화하며 두 번째 선택에서 R4까지 건너뛸 수 있다. 구현 단계에서는 다음 순서로 교정한다.

1. 후보 생성기가 각 체크포인트의 **정확한 다음 티어**만 반환하도록 변경한다.
2. 1스테이지 보스 뒤 R2, 2스테이지 보스 뒤 R3 체크포인트를 유지한다.
3. 3스테이지 고룡 뒤 이벤트를 결전 준비 장면으로 전환해 R4 또는 보급 선택을 제공한다.
4. 14개 무기군 모두 `R1→R2→R3→R4` 실제 경로가 한 시드 이상에서 완주되는지 자동 검사한다.
5. 속성·예리도·무기 특수 데이터가 전투 인스턴스에 실제 적용되는지 무기군별 행동 테스트로 검증한다.

## 6. 랜덤 이벤트

이벤트는 새 미니게임을 계속 추가하는 방식이 아니라, 소수의 공통 선택 문법에 외형과 보상만 바꾸는 방식으로 만든다.

### 공통 선택 문법

- `안전하게 / 욕심낸다`
- `산다 / 떠난다`
- `강화 / 보급`

선택 시간은 짧게 유지하고 무응답이면 현재 체력·아이템·제니를 평가한 AI 추천안을 실행한다.

이벤트 노드는 반드시 `행선지 투표 → 장면 진입 → 행동·상품 선택 → 자원 정산`의 두 단계로 진행한다. 행선지 투표만으로 보상을 즉시 지급하지 않으며, 각 장면은 2~3개의 비용·위험·보상이 다른 실제 행동을 제공한다.

### 초기 이벤트 풀

| 이벤트 | 핵심 선택 | 주요 결과 |
|---|---|---|
| 대장장이 | 무기 후보 2개 중 강화 | 무기명·희귀도·성능 변경, 제니 소비 |
| 채집 | 안전 채집 / 깊은 채집 | 아이템·제니·인장 획득, 깊은 채집은 난입 가능 |
| 아이루 상단 | 회복 / 범용 보급 | 물약·가루·수레 관련 상품 |
| 테토루 교역 | 전술 도구 / 정보 | 함정·섬광·다음 전투 정보 |
| 보와보와 마을 | 생존 보급 / 냉기 계통 보상 | 회복·방어·특수 재료 성향 |
| 가자부 마을 | 폭발물 / 위험한 거래 | 폭탄·공격 자원, 손실 가능성 |
| 몬스터 난입 | 쫓기 / 영역다툼 유도 / 교전 | 자원 손실, 추가 인장, 강한 보상 |

아이루·테토루·보와보와·가자부는 각자 완전히 다른 상점 시스템을 갖지 않는다. 같은 거래 엔진을 공유하고 상품 성향·대사·연출만 다르게 해 복잡도를 억제한다.

### 난입 규칙

- 난입은 우선 **깊은 채집**을 선택했을 때만 발생한다.
- 난입 몬스터는 서식지, 스테이지 위험도, 현재 파티 체력과 충돌하지 않는 후보에서 뽑는다.
- 난입은 항상 전투가 아니다. 영역다툼으로 기존 몬스터에게 피해를 주거나, 아이템을 버리고 도주하거나, 추가 보상을 노리고 교전할 수 있다.
- 같은 런에서 같은 난입 몬스터는 반복하지 않는다.
- 고룡과 초대형 몬스터는 일반 난입 풀에 넣지 않는다.

초기 조사 후보는 이블조·바젤기우스 계열을 중심으로 시작한다. 아종·특수개체와 아이스본의 광역 출현 몬스터는 별도 티어로 검증한다. 이름만 보고 난입형으로 분류하지 않고, 원작의 탐색 출현 지역·퀘스트 동시 출현·영역다툼 근거를 기록한 뒤 채택한다.

## 7. 몬스터 티어와 보상

몬스터의 `소형/중형/대형/고룡급/초대형`은 원작 종 분류와 별개의 런 난이도 역할이다.

| 런 티어 | 구성 원칙 | 주 보상 |
|---|---|---|
| 소형 | 3~5마리 다대다, 짧은 전투 | 소모품·소액 제니 |
| 중형 | 낮은 체력의 대형종 또는 강한 무리 | 제니 1·보급·계통 인장 |
| 대형 | 완전한 부위·분노·필살기 전투 | 제니 2·무기 강화 후보 |
| 고룡급 | 함정 면역과 강한 환경 기믹 | 제니 3·R3/R4 계통 해금 |
| 초대형 | 전용 화면·다단계 부위 공략 | 런 최종 판정 |

각 티어는 체력 배율만 바꾼 복제판이 아니어야 한다. 전투 시간, 패턴 수, 부위 목표, 광역 압박과 보상 구조가 함께 달라져야 한다.

## 8. 데이터 수집 계획

런타임 `data/hunt/wilds-game-reference.json`은 최종 무기 439개만 유지하지만, 로컬 근거 DB에는 설치본에서 추출한 14개 무기군 1,188개 제작 노드와 부모·분기·소재·계통을 보존한다. 여정 강화는 DB→소형 런타임 카탈로그 변환 및 밸런스 규칙 검증 전까지 기존 임시 무기고 선택을 유지하며, 원본 관계를 직접 읽는 다음 단계에서 실제 제작 트리로 승격한다.

현재 추적 데이터에는 Wilds 최종 무기 439개의 희귀도·공격·예리도·속성·트리 위치가 들어 있다. 그러나 제작 로그라이크에 필요한 **전체 중간 노드, 선행 무기, 분기, 제작·강화 소재**는 아직 런타임 데이터에서 제외되어 있다.

다음 필드를 14종 전체에 대해 수집한다.

- 게임·버전·원본 ID
- 무기군·트리 ID·행·열
- 원문명·공식 한국어명
- 선행 무기와 후속 분기
- 직접 제작 가능 여부
- 제작·강화 제니와 소재
- 희귀도·공격력·회심·방어 보너스
- 속성·상태이상·예리도·슬롯·스킬
- 포격·병·탄·코팅·선율 등 무기별 데이터
- 아이콘·외형 참조와 데이터 근거

수집 순서는 다음과 같다.

1. 설치된 게임 파일의 무기·아이템·한국어 텍스트 테이블 인벤토리
2. ID 연결과 전체 제작 그래프 구성
3. 기존 Wilds 참조 데이터 및 API와 대조
4. 14종의 끊어진 분기·순환·고아 노드 검사
5. 로그라이크용 계통 인장 매핑
6. 작은 생성 파일로 런타임 압축

원본 추출물은 계속 `game_extracts/`와 `local_assets/monster_hunter/`에만 두고 커밋하지 않는다. 추적 저장소에는 출처와 필요한 필드만 압축한 생성 카탈로그를 둔다.

## 9. 구현 마일스톤

### M0. 제품 계약 고정 — 현재

- [x] 3스테이지 기본 노드 순서 정의
- [x] 자원·퍽 잠금·무기 성장의 핵심 루프 정의
- [x] 복잡도 제한과 무응답 자동 진행 원칙 정의
- [x] 1런 목표 시간과 실패 보상 확정

목표 런타임은 35~50분이다. 실패 시 영구 능력치는 지급하지 않고, 마지막 정산 노드까지의 무기 경로·퍽 조합·도달 스테이지를 요약 기록으로 남긴다. 재도전은 새 시드와 기본 자원으로 시작해 실패 파밍이 최적 전략이 되지 않게 한다.

완료 기준: 이후 기능 요청을 어느 마일스톤에 넣을지 즉시 판단할 수 있다.

### M1. 전체 무기 제작 데이터

- [x] 설치본에서 전체 무기 노드와 한국어 명칭 추출
- [x] 선행·분기·소재 그래프 생성
- [x] 14종 철·뼈 시작점과 R1~R4 후보 검증
- [x] 몬스터 종을 압축 보상 계통에 매핑
- [x] 재생성 가능한 스크립트와 데이터 계약 테스트 작성

완료 기준: 14종 모두 철·뼈 중 하나에서 시작해 끊김 없이 최소 하나의 R4 무기로 도달한다.

### M2. 런 디렉터와 저장 — 완료

- [x] `HuntRunDirector` 상태 머신
- [x] 15개 노드 덱 생성과 시드 저장
- [x] 체력·아이템·수레·제니·퍽·무기 상태 영속화
- [x] OBS 재연결과 새로고침 복구
- [x] 무응답 AI 기본 선택

완료 기준: 브라우저를 새로고침해도 완료된 노드 다음부터 같은 런을 이어간다.

### M3. 스테이지 1 수직 완성본

- [x] 소형 다대다 → 중형 → 대형 전투 연결
- [x] 전투 사이 자원 유지
- [x] 잠금 1개와 리롤 보상
- [x] 대장장이·채집 이벤트
- [x] 철·뼈 → R2 강화

완료 기준: 채팅 0명과 4명 참여 모두 스테이지 1을 처음부터 끝까지 진행한다.

### M4. 이벤트와 경제

- [x] 제니 `0~9`, 가격 `1~3` 경제
- [x] 공통 거래 엔진
- [x] 아이루·테토루·보와보와·가자부 이벤트 외형
- [x] 채집 위험 선택과 난입
- [x] 난입 후보의 근거·서식지·티어 카탈로그

완료 기준: 이벤트가 공짜 회복 버튼이 아니며, 선택에 따라 다음 두 전투의 자원 상태가 달라진다.

### M5. 무기 성장 완성

- [x] 계통 인장 보상
- [x] 실제 이름·희귀도·외형·예리도 변경
- [x] 후보 최대 2개 추천
- [x] 14종 특수 데이터 승계
- [x] 다른 무기군 분기와 존재하지 않는 임의 무기명 차단

완료 기준: 어떤 무기군도 강화 후 고유 자원이나 액션 조건을 잃지 않는다.

### M6. 스테이지 2·3과 보스

- [x] 스테이지 2 잠금 상한 2
- [x] 고룡급 전투와 함정 면역
- [x] 스테이지 3 잠금 상한 3
- [x] 초대형 몬스터 전용 다단계 전투
- [x] 런 승리·패배 결과와 핵심 자원 회고

완료 기준: 최종 화면이 생존 여부뿐 아니라 완성 퍽 연계, 무기 경로와 결정적 자원 선택을 요약한다.

### M7. 방송용 완성도와 균형

- [ ] 1920×1080 전 노드 UI 검수

정적 1920×1080 레이아웃·채팅 하단 15%·명령어 비생략 계약은 통과했다. 실제 브라우저 렌더 검수는 로컬 주소 사용이 브라우저 정책에서 차단되어 보류하며, 우회하지 않는다.
- [x] 무참여·평균 참여·최대 참여 시뮬레이션
- [x] 14종 무기·성향·퍽 조합 편향 검사
- [x] 런 길이·이벤트 반복·회복 과잉 조정
- [x] 오디오 큐 중복과 장시간 메모리 누수 검사

완료 기준: 스트리머 개입 없이 반복 실행해도 런이 멈추지 않고, 시청자가 현재 선택과 누적 성장을 화면만 보고 이해한다.

## 10. Light 실행 보드

Light 버전은 아래 카드를 **위에서 아래로 한 장씩** 처리한다. 한 카드에서 지정하지 않은 전투 밸런스·애니메이션·오디오를 함께 리팩터링하지 않는다. 각 카드 완료 시 체크박스, 변경 파일, 통과한 명령을 이 문서에 기록한다.

### 공통 실행 규칙

1. 시작할 때 `git status --short`로 사용자 변경을 보존하고, 카드의 입력 파일만 읽는다.
2. 생성 데이터가 필요하면 먼저 기존 카탈로그와 생성 스크립트를 확인한다. 원본 추출물을 런타임에 직접 싣지 않는다.
3. 한 카드의 제품 상태 변경은 최대 3개 소유 파일로 제한한다. 테스트·문서 파일은 이 제한에서 제외한다.
4. 저장 스키마는 추가 마이그레이션만 허용한다. 기존 프로필 행을 지우거나 DB를 재생성하지 않는다.
5. 집중 테스트가 실패하면 다음 카드로 가지 않는다. 실패 원인과 재현 입력을 체크박스 아래에 남긴다.
6. UI 카드는 1920×1080에서 채팅 하단 15%를 침범하지 않는 화면 확인까지 해야 완료다.
7. 수치 밸런스는 승률 목표로 자동 교정하지 않는다. 4인 파티의 멈춤·극단값·자원 고갈 여부만 검사하고 최종 승률 판단은 사용자에게 남긴다.

### L0. 명령과 개인 프로필 기반 — 완료

입력: `GameCommandMatcher`, `HuntInitializer`, 기존 OBS 동반 서버, 퍽·무기 인스턴스 카탈로그.

- [x] `!수렵/!토벌 → single`, `!몬헌 → journey`를 안정 필드로 파싱
- [x] 여정 명령에서 임의 몬스터·무기 토큰을 경로 입력으로 사용하지 않음
- [x] `.runtime/hunt-profiles.sqlite` 지연 생성과 한 행/시청자 upsert
- [x] 로드아웃 진입 복원, 변경 지연 저장, 출발 직전 확정 저장
- [x] NPC 제외, 서버 장애 시 랜덤 로드아웃 폴백
- [x] 시작·중단·도움말 명령을 단일 카탈로그로 통합
- [x] 프로필 스키마·제한·엔드포인트 기본값을 단일 계약으로 통합
- [x] DB 스키마 버전 1과 SQLite 비가용 시 동반 서버 기능 격리

검증: `npm run test:hunt-profiles`, `node tests/message-router.test.js`, `node tests/hunt-monster-command-selection.test.js`, `node tests/hunt-loadout-mutation.test.js`, `node tests/chzzk-companion.test.js`.

### L1. 활성 런 저장 스키마 — 완료

소유 파일: `tools/hunt-profile-store.js`, 새 `js/effects/hunt/HuntRunState.js`, 동반 서버 API.

- [x] `hunt_runs` 테이블을 추가하되 프로필 테이블과 수명 분리
- [x] 채널당 활성 런은 최대 1개, 완료 런은 요약 1개만 보존
- [x] `runId`, `seed`, `status`, `stageIndex`, `nodeIndex`, `revision`, `updatedAt` 검증
- [x] 네 헌터의 현재 HP·아이템·무기 ID·퍽 ID·잠금 슬롯과 파티 수레·제니 저장
- [x] JSON 페이로드를 32KiB 이하로 제한하고 알 수 없는 필드를 버림
- [x] 레코드 수정은 예상 revision이 일치할 때만 허용해 늦은 저장이 최신 체크포인트를 덮지 않게 함

완료 기준: 임시 DB에서 생성→저장→프로세스 재개→동일 상태 복원, 잘못된 revision 거부, 기존 프로필 보존 테스트 통과.

### L2. 여정 디렉터 껍데기 — 완료

소유 파일: 새 `HuntRunDirector.js`, `HuntEffect.js`, `HuntResultPresenter.js`.

- [x] `!수렵`은 기존 단일 흐름으로 즉시 분기하고 런 DB를 읽지 않음
- [x] `!몬헌`은 활성 런을 복원하거나 시드가 있는 새 런을 생성
- [x] `HuntEffect`는 전투 노드의 입력과 결과만 디렉터에 반환
- [x] 승리 결과는 다음 노드로, 패배·수레 소진은 런 종료로 전이
- [x] 페이지 새로고침 후 마지막 **정산 완료 노드의 다음 노드**에서 재개
- [x] 동일 결과 콜백이 두 번 와도 nodeIndex가 한 번만 증가

완료 기준: 가짜 전투 결과로 15노드 상태 전이를 완주하고 단일 수렵의 기존 라이프사이클 테스트가 그대로 통과.

### L3. 시드 기반 15노드 덱 — 완료

소유 파일: 새 `HuntJourneyCatalog.js`, 기존 몬스터 티어 데이터, `HuntRunDirector.js`.

- [x] 스테이지별 고정 노드 타입 배열을 코드 상수로 정의
- [x] 몬스터 후보는 `small/medium/large/elder/colossal` 검증을 통과한 것만 사용
- [x] 한 런에서 동일 개체 반복 억제, 후보 부족 때만 명시적 반복 허용
- [x] 이벤트 카탈로그에서 2~3개 후보를 생성하고 같은 이벤트 연속 노출 억제
- [x] 시드와 카탈로그 버전이 같으면 항상 같은 덱 재생성
- [x] 저장에는 전체 몬스터 객체가 아니라 노드 ID·monsterId·eventId만 기록

완료 기준: 시드 100개에서 항상 3스테이지 15노드, 올바른 티어 순서, 빈 후보 없음, 결정성 테스트 통과.

### L4. 전투 사이 자원 승계

소유 파일: `HuntRunDirector.js`, `HuntInitializer.js`, `HuntEngine.js`의 명시적 입출력 경계.

- [x] 전투 시작 때 런 스냅샷을 헌터 런타임 상태로 물질화
- [x] 전투 종료 때 HP·물약·가루·함정·폭탄·수레만 정산
- [x] ATB, 기절, 속성 상태이상, 귀인화 같은 전투 임시 상태는 저장하지 않음
- [x] 숫돌·탄·일반 정비는 노드 사이 자동 복구
- [x] 수레 복귀 재보급과 런 재고 증가가 중복되지 않게 한 소유자만 정산
- [x] 단일 수렵은 지금처럼 독립된 기본 보급으로 시작

완료 기준: 두 전투 연속 픽스처에서 첫 전투 소모품과 HP가 두 번째에 정확히 이어지고 일시 상태는 사라짐.

### L5. 이벤트 공통 엔진과 최소 UI

소유 파일: 새 `HuntJourneyEventEngine.js`, 새 `HuntJourneyRenderer.js`, `HuntRunDirector.js`.

- [x] 선택지는 2~3개, 투표 시간은 60초이며 인간 참가자 전원 투표 시 즉시 확정, 무응답이면 결정적 AI 추천 실행
- [x] 대장장이 강화: 현재 무기군에 맞는 강화 후보 최대 2개
- [x] 채집: 안전/깊은 채집, 깊은 채집만 난입 판정 훅 제공
- [x] 거래: 회복/전술 상품을 공통 가격 1~3으로 처리
- [x] 투표 동률은 시드 기반 고정 규칙으로 결정
- [x] 정산 후 한 번 저장하고 다음 노드로 전이; 투표 틱마다 저장 금지
- [x] 최초 참가 모집은 시작 사냥감을 자동 확정하고, 다음 사냥감 투표는 파티 결성 이후 노드부터 표시

완료 기준: 채팅 0명과 4명 픽스처가 모두 멈추지 않고 각 이벤트를 통과하며 제니가 0 미만·9 초과가 되지 않음.

### L6. 스테이지 1 수직 완성

소유 범위: L1~L5의 공개 API만 사용하고 새 우회 상태를 만들지 않는다.

- [x] 소형→이벤트→중형→이벤트→중형→이벤트→대형 실제 연결
- [x] 스테이지 1 잠금 상한 1과 리롤 보상 연결
- [x] 낮은 레어도 시작 무기에서 검증된 동일 무기군 후보로 강화
- [x] 중단·새로고침·채팅 단절 복구
- [x] 결과 화면에 진행도와 남은 핵심 자원 표시

완료 기준: 무참여/참여 2경로를 각 10회 반복해 멈춤·중복 정산·자원 복제 0건, 1920×1080 UI 확인.

### L7. 스테이지 2·3 확장

- [x] L6 안정화 전 시작 금지
- [x] 잠금 상한을 스테이지 진입 체크포인트에서 2, 3으로 증가
- [x] 고룡급 함정 면역과 초대형 전용 전투 계약 재사용
- [x] 런 완료 뒤 활성 런 제거, 요약만 남기고 영구 능력치 상승 없음
- [x] 무기 트리·난입 후보는 근거 데이터가 확보된 항목만 개방

완료 기준: 전체 15노드 완주와 실패 재시작, OBS 새로고침 복구, 전체 회귀·자산 검증 통과.

## 11. 저장 스키마 계약

프로필과 런은 같은 SQLite 파일을 쓰되 결합하지 않는다.

| 테이블 | 키 | 갱신 시점 | 삭제 정책 |
|---|---|---|---|
| `hunt_profiles` | 해시된 viewer identity | 로드아웃 실제 변경, 출발 | 자동 삭제 없음 |
| `hunt_runs` | `channelKey` 1행 | 노드 생성·정산·선택 확정 | 완료·포기 시 활성 행 제거 |
| `hunt_run_summaries` | `runId` | 런 종료 1회 | 최신 1개만 유지 |

`HuntRunState` 최소 형태는 다음과 같다.

```json
{
  "schemaVersion": 1,
  "catalogVersion": 1,
  "runId": "uuid",
  "seed": 123456789,
  "status": "event",
  "stageIndex": 0,
  "nodeIndex": 1,
  "carts": 3,
  "zenny": 2,
  "lockLimit": 1,
  "rerolls": 1,
  "party": [],
  "nodes": [],
  "revision": 4
}
```

저장하지 않는 값: DOM 상태, 타이머 ID, Audio 객체, 애니메이션 클래스, 현재 ATB, 대미지 팝업, 전투 틱, 전체 몬스터·무기·퍽 레코드. 복구 시 카탈로그 ID와 체크포인트만으로 다시 만든다.

## 12. 이번 초안에서 의도적으로 보류한 것

- 계정 영구 성장과 해금
- 방어구 제작 트리
- 원작 소재 수십 종의 개별 수량 관리
- 세 명 이상의 선택지를 동시에 제시하는 이벤트
- 모든 마을마다 별도 상점 규칙
- 몬스터마다 독립된 전용 화폐
- 한 런에서 무기군 자체를 자유롭게 교체하는 기능

이 항목들은 기본 3스테이지 런이 재미있고 안정적으로 완주된 뒤에만 재검토한다.

## 13. 조사 근거와 다음 확인 대상

- 런타임 `data/hunt/wilds-game-reference.json`: 34종 대형 몬스터, 최종 무기 439개, 아이템 773개. 비공개 `mh-wilds.sqlite`: 전체 제작 노드 1,188개, 연결 961개, 소재 요구 3,615개, 계통 47개를 별도 보존.
- [Monster Hunter Wilds API reference](https://docs.wilds.mhdb.io/): 무기 제작 데이터에 선행 무기, 후속 분기, 제작·강화 소재와 제니 필드가 존재함을 확인하는 대조 자료.
- [Kiranico Deviljho expeditions](https://mhworld.kiranico.com/en/monsters/4rATd/deviljho): 여러 지역 탐색 출현과 보상·부위 자료를 가진 초기 난입 후보 근거.
- [Kiranico Savage Deviljho expeditions](https://mhworld.kiranico.com/en/monsters/B02Tk/savage-deviljho): 마스터 랭크 전 지역 출현을 포함한 상위 난입 티어 검토 자료.

다음 조사에서는 바젤기우스 계열, 라잔 계열, 아이스본의 광역 출현 후보를 같은 형식으로 검증하고, `서식지·등장 랭크·영역다툼 상대·난입 행동·도주 비용·보상 계통`을 하나의 카탈로그로 만든다.
