# BubbleChat 운영 가이드

## OBS 브라우저 소스 권장값

- 로컬 파일: `index.html`
- 너비 × 높이: `1920 × 1080`
- FPS: `60`
- 사용자 지정 CSS: 불필요
- 장면이 비활성화될 때 소스 종료: **끔**
- 장면이 활성화될 때 브라우저 새로고침: **끔**

브라우저 소스를 계속 살려 두어야 채팅, 실행 중인 게임, 효과 대기열이 유지됩니다. 치지직 방송이 다시 시작되어 내부 채팅 세션이 바뀌어도 페이지를 새로고침하지 마십시오. 게이트웨이가 최대 30초 단위로 새 세션을 확인하고, 새 소켓 인증이 끝난 뒤 기존 소켓을 교체합니다.

## 방송 전 점검

프로젝트 폴더에서 다음 두 명령을 실행합니다.

```powershell
npm.cmd test
npm.cmd run verify
```

첫 명령은 채팅 연결, 방송 재시작 세션 교체, 게임 수명주기, 오디오, 1080p 배치 계약을 검사합니다. 두 번째 명령은 코드 인코딩과 문법뿐 아니라 1,400개 이상의 실제 미디어 경로와 채팅 영상 명령을 검사합니다.

## 연결 문제 판단

- 최초 연결은 캐시된 세션을 우선 사용합니다.
- 캐시가 잘못되었거나 인증이 실패하면 자동으로 폐기하고 현재 방송 세션을 다시 찾습니다.
- WebSocket이 열렸다는 사실만으로 연결 완료로 판단하지 않으며, 치지직 인증 응답을 받아야 연결 완료가 됩니다.
- 연결이 끊겨도 페이지, 채팅 버블, 게임, 효과 상태는 유지됩니다.
- 정상 방송 중 2분 동안 프로토콜 패킷이 없으면 세션을 다시 확인합니다.

OBS 새로고침은 파일 자체를 업데이트했을 때만 사용합니다. 방송 재시작이나 일시적인 네트워크 장애에는 사용하지 않습니다.

## 음량 운영

원본 미디어는 수정하지 않습니다. 새 SFX, BGM, 영상 파일을 추가하거나 교체한 뒤 아래 명령을 한 번 실행합니다.

```powershell
npm.cmd run analyze:audio
npm.cmd run verify
```

파일별 측정값은 `js/audio-levels.generated.js`에 저장됩니다. OBS의 `file://` 재생에서는 영상과 BGM을 Web Audio에 강제로 연결하지 않고 네이티브 볼륨을 사용하므로 무음 CORS 문제를 피합니다. 컴프레서는 여러 효과가 겹칠 때의 피크 보호를 맡고, 체감 음량 균일화는 파일별 측정 보정이 담당합니다.

## 백업과 복구

리뉴얼 전 백업은 `backup_v1`에 있습니다.

- Git 번들: 전체 커밋 기록과 브랜치 복구용
- 추적 소스 ZIP: 당시 코드의 즉시 열람용

현재 리뉴얼은 `codex/full-rebuild` 브랜치에 기능별 작은 커밋으로 나뉘어 있습니다. 문제가 생기면 전체 폴더를 덮어쓰지 말고, 먼저 해당 기능 커밋 전후를 비교하십시오. 사용자 미디어와 기존 미추적 복구 파일은 리뉴얼 과정에서 삭제하거나 이동하지 않았습니다.

## OBS 권장 실행 방식

최초 한 번만 다음과 같이 등록합니다.

1. OBS의 `도구 → 스크립트`를 엽니다.
2. `+` 버튼으로 `obs/bubblechat-companion.lua`를 추가합니다.
3. OBS 브라우저 소스 URL을 `http://127.0.0.1:17890/index.html`로 지정합니다.
4. 브라우저 소스 크기는 `1920 × 1080`으로 유지합니다.

이후 OBS를 실행하면 로컬 보조 실행기가 창 없이 자동 시작되고, OBS 종료 또는 스크립트 제거 시 자동 종료됩니다. OBS가 비정상 종료되어도 보조 실행기가 OBS 프로세스를 감지해 수 초 안에 함께 종료됩니다. OBS를 사용하지 않을 때는 관련 프로세스가 남지 않습니다.

`START_OBS_OVERLAY.bat`는 OBS 스크립트 등록이 풀렸거나 장애를 진단할 때만 사용하는 수동 복구 수단입니다. 실행기가 꺼져 있어도 기존 직접 연결 및 외부 폴백 경로는 유지됩니다.
